<h1><%= @board.name %></h1>
<p>Generated by: <%= @board.email %></p>
<p>Created at: <%= @board.created_at.strftime("%B %d, %Y %H:%M") %></p>

<%= button_tag 'Conceal Board', id: 'conceal-board-btn', class: 'btn btn-dark', type: 'button' %>
<div class="mb-5"></div>

<table class="table table-bordered">
  <% @board.grid.each do |row| %>
    <tr>
      <% row.each do |cell| %>
        <td class="board-cell">
          <%= cell == 'M' ? '💣' : cell %>
        </td>
      <% end %>
    </tr>
  <% end %>
</table>



<div class="mb-5"></div>
